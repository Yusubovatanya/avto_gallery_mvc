Run `npm install `

Run `node index.js` for a dev server. Navigate to `http://localhost:3000/`. The app will automatically reload if you
change any of the source files.

"user": {
    "login": "admin@gmail.com",
    "password": "123456789"
  }